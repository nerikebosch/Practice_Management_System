services:
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_DB: medpulse_db
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: